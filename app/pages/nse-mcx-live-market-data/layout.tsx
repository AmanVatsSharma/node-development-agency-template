// TEMPORARILY DISABLED — NSE/MCX Live Market Data page hidden from site.
// Original page/layout/metadata moved aside. To restore, restore _disabled-page.tsx and recreate original layout/metadata.

import { ReactNode } from 'react';

export default function NseMcxMarketDataLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
